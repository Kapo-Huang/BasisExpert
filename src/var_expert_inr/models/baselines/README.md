# Shared-engine baselines

These are model implementations that use the common config, data, training,
checkpoint, and evaluation stack. The `baselines` name describes their role in
this repository without making a time-sensitive “state of the art” claim.

